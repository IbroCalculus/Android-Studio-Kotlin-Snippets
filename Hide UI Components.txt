
Assuming using imageView


private void SHOW_Method() {
        imageView.setVisibility = VISIBLE
    }

// Hide, and UI Interface disappears
    private void GONE_Method() {
        imageView.setVisibility = GONE
    }

// Hide, but UI Interface remains
    private void HIDE_Method() {
        imageView.setVisibility = INVISIBLE;
    }